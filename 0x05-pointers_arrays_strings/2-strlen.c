#include "holberton.h"

/**
 *_strlen -  function counts the number of characters in a given string
 *@s: is the pointer
 *Return: len
 */
int _strlen(char *s)
{
	int len;

	for (len = 0 ; s[len] != '\0' ; len++)
	{
	}
	return (len);
}
